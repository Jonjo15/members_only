<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title>Posts</title>
    
</head>
<body>
    <ul>
        <% for (var i = 0; i < list_posts.length ; i++) { %>
            <li>
                <div class="post">
                    <% if (currentUser.isMember) {%>
                    <h2><%= list_posts[i].title %> by <%= list_posts[i].user.username %></h2>
                    <%} %>
                    <p><%= list_posts[i].body%></p> 
                    <% if (currentUser.isAdmin) {%>
                        <a href=<%=list_posts[i].url %>>Delete Post</a>
                    <%} %>
                        
                    
                </div>
                 
            </li>
        <% } %>
    </ul>
</body>
</html>